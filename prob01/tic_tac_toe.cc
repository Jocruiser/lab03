#include <iostream>
#include <vector>

int CheckGame(std::vector<std::vector<int>>& game) {

  // Your code here to determine if there was a winner, a draw, or if the
  // game can continue.


int main()
{

 vector<int> numbers = {-1, 0, 1, 2};
  
  const int SIZE = 4
  
  // define vector to hold 4 int values
  vector<int> numbers(SIZE);
  
 for (int x = 0; x < game.size(); x++) {
   for (int y = 0; game.size(); y++) {
   x = game.size; 
   break;
   
   } 

}
  
 
 
  // store values 0 through 2 in the vector  
  for (int index = -1; index < numbers.size(); index++)
    numbers [index] = index;
  
  // display vector element  
  for (auto element : numbers)
    cout << element << " ";
  cout << endl; 
  
  
  return result; 


}
